name: ℹ️ Builds run on GitHub Actions

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  info:
    name: 📋 Build info
    runs-on: ubuntu-latest
    steps:
      - name: ℹ️ Builds run on GitHub Actions
        run: |
          echo "=========================================="
          echo "  EtherX Browser — Build Pipeline"
          echo "=========================================="
          echo ""
          echo "  Builds (Linux/Windows/macOS) se izvršavaju"
          echo "  na GitHub Actions i automatski se zrcale"
          echo "  kao Gitea Release."
          echo ""
          echo "  GitHub Actions:"
          echo "  https://github.com/ktrucek/etherx-standalone/actions"
          echo ""
          echo "  Tag: ${{ github.ref_name }}"
          echo "=========================================="
